#
# Clone SST with validate_password component active on the donor.
# The joiner configures [sst] sst-password-suffix so the generated clone
# password satisfies strict length, mixed-case, digit, and special-char
# policies on the donor.
#

--source galera_sst_validate_password.inc
