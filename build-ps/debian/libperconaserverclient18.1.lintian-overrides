# We are libmysqlclient, so of course we look like it.
embedded-library binary: usr/lib/*/libmysqlclient.so.18.1.0
embedded-library binary: usr/lib/*/libmysqlclient_r.so.18.1.0

# no wildcards? :(
embedded-library binary: usr/lib/x86_64-linux-gnu/libmysqlclient.so.18.1.0
